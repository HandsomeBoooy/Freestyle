extends layout

block content
  link(rel="stylesheet", href="../stylesheets/homepage.css")
  h1 #{title}
  div.main-container
    div.resume-entrance#resume-link
      |Go To Miata's Resume
  script.
    (() => {
      document.getElementById("resume-link").addEventListener("click", () => {
        location.replace("./resume");
      });
    })();